# raw

- `_inbox/`：未分域
- `bookmarks/`：`github.md` / `sites.md` 表格
- `articles/<作者>/<标题>.md`：成文（frontmatter 须有 `url:` 便于查重）
- `articles/_media/<slug>/`：配图（正文用 `../_media/<slug>/...`）
- **无**根目录认领表；认领=知识页 `sources`/Related 链到 raw
