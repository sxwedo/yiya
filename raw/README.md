# raw

- `_inbox/`：未分域剪藏
- `bookmarks/`：书签列表（`github.md` / `sites.md`），追加条目，不按项目建 stub 目录
- `articles/<人话标题>.md`：成文型原文（**扁平**，无 YYYY/MM、无 slug 目录）
- `articles/_media/<slug>/`：配图（相对链接 `./_media/<slug>/...`）
- 认领见根目录 `raw-manifest.yaml`（仅成文型；书签不进 per-link claim）
